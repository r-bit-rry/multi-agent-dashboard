# Server Configuration
PORT=3001
WS_PORT=8766
NODE_ENV=development

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database
DB_PATH=./events.db

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000

# Anthropic API for Task Summaries (Haiku 3.5)
# Get your API key from https://console.anthropic.com/
# This key is used server-side only to generate summaries for all users
# The key is never exposed to clients
ANTHROPIC_API_KEY=sk-ant-api03-YOUR-KEY-HERE

# x402 Cryptocurrency Payment Integration (OPTIONAL - Disabled by default)
# See x402-integration-guide.md for setup instructions
# Get your wallet address from MetaMask, Coinbase Wallet, or similar
X402_ENABLED=false
X402_WALLET_ADDRESS=0xYourWalletAddressHere
X402_NETWORK=base
X402_CURRENCY=USDC
X402_DEBUG=false